#include<stdio.h>
#include<string.h>
#include<ctype.h>


int main() {

	//Declare a char array named 'password' with 10 elements to store the user's input
	//CODE:

	//Declare 3 int variables all initialized to 0 as flags for lowercase/uppercase letters and a digit. 
	//CODE:

	printf("Please enter a strong password!\n");
	printf("Must contain uppercase, lowercase letters and digits.\n");
	printf("No more than 9 characters. No symbols\n");
	printf("Make your password:\n");

	//call scanf_s() and pass 3 arguments: string with number of slots, variable password, and sizeof() with arg...
	//CODE:

	//Create for loop with a counter var initialized to 0, condition i < string length() with password arg. Increment by 1
	//CODE:

	//Make a THREE nested if statements which check lowercase/uppercase letters and digits in the 
	//password we call the islower(),is... and is... functions  (password each i).
	//CODE:

	//Use the second if isupper() function with same protocol.
	//CODE:

	//Use an if statement in same way as above the above if's using isdigit().
	//CODE:

	//Use an an if statement with the condition lowerCase AND upperCase AND digit as arguments.
	
	//Call the print function with a sentence saying they have a strong password.
	//CODE:

	//Use an else with printf() to say password is weak.
	//CODE:

}