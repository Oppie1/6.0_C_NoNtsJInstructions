#include<stdio.h>
#include<stdlib.h>




int main() {

	//Declare an uninitialized int variable to hold the user's age.
	//CODE:

	//Declare a character variable to hold the user's gender. 
	//CODE:

	//call printf() function to prompt user to enter their age.
	//CODE:
	printf("How old are you?\n");

	//call scanf_s() function to scan in the users age.
	//CODE:

	//Call printf() to request the users age.
	//CODE:
	printf("\nWhat is your gender m/f?\n");

	//Read in a single character from the console and stores it in memory address of 'gender'.
	//CODE:

	//Use if condition to test if user is above 18 and print you may enter if they are.
	//CODE:

	//Use nested if/else below to determine whether person is a male or female.
	//CODE:

	//Make a nested else to the first if that executes and says a female has entered the website.
	//CODE:

	//Make an else condition that prints that the person is not old enough to enter the website.
	//CODE:

}