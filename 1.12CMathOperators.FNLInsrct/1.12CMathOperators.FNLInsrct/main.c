#include<stdio.h>
#include<stdlib.h>



int main()

{

	//Declare 4 int variables a b and c initialize two being positive, one negative.Then create one to hold weight.
	//CODE:

	//Call a printf() that takes ints in expression where it divides the two positive variable as its second parameter. 
	//CODE:

	//Call a print function in same way as above just use a negative.
	//CODE:

	//Declare two float variables initialized to decimal .00 numbers
	//CODE:

	//Print a message to screen that divides the two floats together.
	//CODE:

	//Print the same expression as above with %.3f.
	//CODE:

	//Declare a float variable named weight and initialize it to 7.
	//CODE:

	//Call print() that uses sentence about 4 watermelons weighing _ pounds being _ more pounds than you thought. Use modulus operator.
	//CODE:

}