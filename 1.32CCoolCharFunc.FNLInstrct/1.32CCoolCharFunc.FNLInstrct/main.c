#include<stdio.h>
#include<ctype.h>


//Character classification functions work with an int variable, even when that variable holds a character
//literal (e.g 'A'). or a symbol e.g. '$'.

int main()
{

	//Declare int variable named exampleDataType. Initialize it to '$';
	//CODE:

	//Use if statement with isalpha() to test whether alphabetic char was entered. Print message to screen
	//using correct syntax.
	//CODE:

	//Make a else if statement that uses isdigit() and print statement.
	//CODE:

	//Use follow up else statement that functions as a catch-all other possibilities. Print message.
	//CODE:
}