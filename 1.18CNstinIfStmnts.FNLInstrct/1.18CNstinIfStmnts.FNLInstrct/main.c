#include<stdio.h>
#include<stdlib.h>



int main()
{
	//Declare an uninitialized int var to store the user's age.
	//CODE:

	//Declare an uninitialized character variable to store the user's gender.
	//CODE:

	//Prompt the user to enter their age.
	//CODE:
	printf("Please enter you age.\n");

	//Call scanf_s() to store age value in int age.
	//CODE:

	//Prompt user for their gender using a call printf function.
	//CODE:
	printf("What is your gender?\n");

	//Call scanf_s() to scan in user data that has 3 parameters. makes sure user ONLY enters 1 character 
	//CODE:

	//Create outer if statement with the condition if the persons age is greater
	//than or equal to 18 to print the statement enter website to screen.
	//CODE:
	//Use nested if statement check if the gender is male 'm' to connect entry sentence.
	//CODE:

	//Use if condition within the nest to check if the user is a female along with sentence to connect entry sentence.
	//CODE:

	//Write if condition to test if the person is less than 18. If they
	//are print a statement that they cannot enter the website to the screen.
	//CODE:

}