#include<stdio.h>
#include<math.h>




int main() {

	//Declare an uninitialized int counter variable named i.
	//CODE:

	//Declare an uninitialized int variable named diceRoll to store that value.
	//CODE:

	//Create for loop with i initialized to 0, condition less than 5, increment i 1.
	//Print to screen digit on new line each iteration. Must use rand() with no parameters.
	//CODE:

	printf("\n\nPrint a Random Dice Roll to the Screen (1-10 total rolls:) \n");

	//Create a for loop that initializes i to 5; a condition i < 10 and increments i by 1
	//CODE:

		//Assign var dice roll to expression using the rand() function with no parameters modulus % 6 and add 1 to human it.
		//CODE:

		//Call printf() with sentence and new line char that corresponds to second arg being the var diceRoll.
		//CODE:
}