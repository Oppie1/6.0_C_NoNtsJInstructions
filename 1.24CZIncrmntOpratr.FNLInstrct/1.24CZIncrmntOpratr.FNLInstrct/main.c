#include<stdio.h>
#include<stdlib.h>


int main()
{
	//Declare 3 int variables.  a, b, answer
	//be initialized to 5 and 10 respectively. answer should be initialized to 0.
	//CODE:

	//Assign answer to the expression incrementing a before it is multiplied by b.
	//CODE:

	//Call printf() with a and 'answer' with a sentence to print a and answer to the screen. 6 and 60
	//CODE:

	//Declare variables to the original values 
	//CODE:

	//Assign answer set to increment a after and then multiply by b.
	//CODE:

	//Call printf() with a and answer with sentence to the screen to show difference with pre increment. 6 and 50
	//CODE:

	printf("\n------Next Demonstration-------\n\n");

	//Declare an initialized variable named tuna and set it to 20
	//CODE:

	//Display the initial value of to the screen
	//CODE:

	//Use pre-increment operator so tuna goes from 20 to 21.
	//CODE:

	//Now show the new value to the screen.
	//CODE:

	//Use the pre-increment again using tuna and printf() to screen.
	//CODE:
	
	//Do the subtraction increment operator -- just to show in example proficiency. And print to screen. 
	//CODE:

	//ASSIGN tuna and to the pre-increment of tuna * 2.
	//CODE:

	//ASSIGN tuna and then tuna to the post-increment operator to see what happens.
	//Multiply by 2 like above.
	//CODE:

}