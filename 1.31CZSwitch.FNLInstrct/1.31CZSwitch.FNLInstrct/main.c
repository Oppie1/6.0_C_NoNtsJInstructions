#include<stdio.h>




int main() {

	//Declare a char variable named grade and initialize it to 'B'
	//CODE:
	// 
	//Use a switch statement with the parameter grade to do this comparing.
	//CODE:

	//case 'grade' then use a colon followed by a print statement stating grade. Then 
	//use the keyword break  after each case. At the end use the default keyword for F.
	//CODE:

	//CODE:

	//CODE:

	//CODE:

	//The default case acts as a catch-all. If the value of grade doesn't match any of the cases above.
	//CODE:
}