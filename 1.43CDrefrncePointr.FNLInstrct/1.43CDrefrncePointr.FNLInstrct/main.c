#include<stdio.h>




int main()
{
	//Declare int var initialized to 19.
	//CODE:

	//Declare pointer var pTuna. Initialize it to memory address of tuna.
	//CODE:

	printf("Address \t\t Name \t Value \n");

	//Call a printf() display  pTuna, "tuna", tuna. Use pointer, string and int frmat Specs.
	//CODE:

	//Call printf() to display difference in location of the memory address in first argument
	//vs the pTuna variable itself in the third argument %p
	//CODE:
	
	//Use the * pointer and pTuna to overwrite the 19 by reinitializing it to 71.
	//CODE:

	printf("\nThe new value of *pTuna AND tuna is now:\n");

	//Call print() to  *pTuna with matching pTuna (*) pointer var to display the value of pTuna AND tuna is to screen.
	//CODE:

}