#include<stdio.h>
#include<stdlib.h>


int main()
{

	//Declare three uninitialized int variables to hold whole number values.
	//CODE:

	//Assign the same value to all three vars in a single statement - chaining the assignment.
	//CODE:

	//Print the values of a, b and c to the console. 
	//CODE:

	//Declare three float variables for 3 ages and 1 to store the result of avg.
	//CODE:

	//Assign age1 and age2 equal to each other and the same value "10".
	//CODE:

	//Call a print function to prompt user to enter an age for age3.
	//CODE:

	//Call a scanf_s() function to read in age3 (mem address).
	//CODE:

	//Calc avg by summing all three ages
	//CODE:

	//Call printf() to show a avg out to the screen.
	//CODE:

}