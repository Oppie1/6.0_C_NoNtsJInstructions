#include<stdio.h>



int main() {


//Declare uninitialized int var for loop counter.
//CODE:

//Declare int array meatBalls with 5 elements. Initialize them to some random values.
//CODE:

//Create for loop initializes i to 0, a condition that i is less than 5 increment i by 1
//CODE:

		//Call print() that will use sentence in first arg, the value of current iteration i digit in second arg, 
		//memory address of each meatball element in 3rd arg and the actual value of each meatball element in 4th argument.
		//CODE:

	//Array name by itself acts as pointer to its first element. Call print() to demonstrate that meatballs array is memory 
	//address of the first element.
	//CODE:

	//Call a print() that gives the int value of meatballs by using *.
	//CODE:

	//Call printf() that adds two to the array and shows memory address at location.
	//CODE:


	//Call printf() wrapping () meatBalls + and make sure you dereference it to show the actual value of the 3rd position.
	//CODE:
}