#include<stdio.h>
#include<stdlib.h>



int main() {

	//Declare uninitialized int var for loop counter as well as a int
	//variable to store how many numbers user wants to average
	//CODE:

	//Declare int var named total and initialize it to 0.
	//CODE:

	//Declare a float variable named average  initialized 0.
	//CODE:

	//Declare uninitialized int pointer named pointsArray.
	//CODE:

	printf("\nHow many numbers do you want to average?\n");

	//Call scanf_s() to bring info into the memory of howMany.
	//CODE:

	//Assign pointsArray int pointer to expression that will request memory from heap at 
	//runtime using typecast and malloc(like last tut but wth var)
	//CODE:

	printf("\nGo ahead and answer those numbers.");

	//Construct for loop that loops through user entries with counter 
	//starting at 0, comparing counter to howMany and increment it by 1.
	//CODE:

		//Scan in user chosen numbers into the memory location of the given element of the pointsArray.
		//CODE:

		//Assign total to expression that takes value entered in points array elements and adds it to the current value..
		//CODE:

	//Assign var average to result of total divided by howMany numbers the user entered. typecast.
	//CODE:

	//Call printf() to display the average to the screen.
	//CODE:
}