#include<stdio.h>

int main() {

	//Declare 3 int variables two uninitialized. A counter variable loop, 
	//howMany and maxAmount initialized to 10 - iterations program can have. 
	//Code:

	//Prompt user asking how many times do they want loop to loop (up to 10)
	//CODE:
	printf("How many times do you want this loop to loop? (up to 10)\n");

	//Scan in the user int selection. Then use the memory address of howMany as the storage for the input.
	//CODE:

	//Write a for loop that uses counter initialized 1. Test against maxAmount to see if less than or equal to 10. Increment by 2.
	//CODE:

		//Print current value of i on its own line each iteration.
		//CODE:

		//Check if i has reached user-specified limit using a if statement.
		//When true, break exits the loop immediately.
		//CODE:
}