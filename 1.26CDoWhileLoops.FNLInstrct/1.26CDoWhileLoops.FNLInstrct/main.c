#include <stdio.h>
#include<stdlib.h>



int main() {

	//Declare 4 float variables initialized to 0 -> grade, scoreEntered, numberOfTests and avg.
	//CODE:

	//Call a print function to let the user know that they should press 0 when they are done entering grades.
	//CODE:
	printf("Press 0 to stop the program\n");

	
	//Create a do condition.
	//CODE:

	//Call a printf() to show the number of tests and the average to the screen.
	//CODE:	

	//Prompt the user to enter a test score
	printf("Enter a test score:\n");

	//Call a function to read in the users input and store it in scoreEntered.
	//CODE:

	//Assign grade to adding the score entered and setting it equal to 
	//the current score entered.
	//CODE:

	//Increment the number of test by one each iteration
	//CODE:	

	//Assign the average a value that accounts for the total of the grades entered
	//divided by the number of test that were input by user.
	//CODE:

	//Make a while loop condition that runs so long as the score entered does NOT equal 0.
	//CODE:

}