#include <stdio.h>
#include<stdlib.h>



int main()

{

	//Declare 1 uninitialized float variable to hold the total revenue. Then Declare 3 int
	//variables to store the initialized priceOfPumpkin, number of sales and days worked.
	//CODE:

	//Assign total revenue to the price of pumpkin times the number of sales. Typecast is necessary so the
	//decimal point is not dropped.
	//CODE:

	//Divide total revenue by the days worked in order to get the average revenue per day.
	//CODE:

	//Call a print function to show a sentence to the screen. Total revenue and avg daily rev to the screen.
	//CODE:
}