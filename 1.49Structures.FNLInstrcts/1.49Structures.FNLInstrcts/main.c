#include<stdio.h>
#include<stdlib.h>
//Include the header file for employee info so the struct definition is available in this file. ""
//CODE:
#include "EmployeeInfo.h";


int main() {

	//Declare two variables of type struct user -> copilot and claude.
	//CODE:

	//ASSIGN copilot and claude user id, age and weight to decimal.
	//CODE:

	//Use a puts() to prompt user for name of user 1
	//CODE:
	puts("What is the user 1's name\n");

	//Use a gets() to read in information from struct copilot -> fullName (.)
	//CODE:

	//Use a puts() to prompt user for name of user 2
	//CODE:
	puts("What is the user 2's name\n");

	//Use gets() to read in information from struct claude -> firstName.
	//CODE:

	//Use two printf() to display information to screen for both copilot and claude. (.)
	//CODE:


}