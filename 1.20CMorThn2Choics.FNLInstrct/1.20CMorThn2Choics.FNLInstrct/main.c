#include<stdio.h>
#include<stdlib.h>


int main()
{

	//Declare three uninitialized float variables representing 3 grades.
	//CODE:

	//Prompt user to enter their 3 grades
	//CODE:
	printf("Enter your 3 grades:\n");

	//Scan in the three grades from the user one at a time.
	//CODE:

	//Assign a new float var avg to the result of calculation using the 3 grades divided by 3. 
	//CODE:

	//Print a sentence to the screen that displays the average to 3 decimal places.
	//CODE:

	//Make if condition that tests if the average is greater than or = to 90. If so print out Grade A to the screen.
	//CODE:

	//Make else/if statement that tests if the average is greater than or equal to 80. If so print Grade B to the screen.
	//CODE:

	//Make another else/if statement that test if the average is greater than or equal to 70. If so then print Grade C to the screen.
	//CODE:

	//Make one more if/else statement with same criteria for Grade D (60)
	//CODE:

	//Use just a "else" statement as a catch all for all F (<60) grades and make a print
	//Statement that says grade F.

}