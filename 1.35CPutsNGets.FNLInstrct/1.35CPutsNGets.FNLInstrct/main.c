#include<stdio.h>
#include<string.h>



int main() {


	//Declare a uninitialized char array to hold the cat's name with 50 elements.
	//CODE:

	//Declare uninitialized char array for cat's favorite food with 25 elements.
	//CODE:

	//Declare array to assemble sentence to hold 7d char initialized to blank quotes.
	//CODE:

		//Prompt the user for the cat's name using puts()
		//CODE:

	//Call gets() to read the full line of input into catsName.
	//CODE:

	//Prompt the user for the cat's favorite food using the puts() function.
	//CODE:

	//Use a gets() to read in user info into the catsFood 
	//CODE:

	//Call strcat_s() to scan user input with 3 args. 1. sentence var, 2. has to do with the size of sentence. 3. var.
	//CODE:

	//Call strcat_s in the same way as you did above but with sentence connecting the 2 pieces of info.
	//CODE:

	//Call in same way you just did above using strcat_s to add in the cats food to the sentence at the end.
	//CODE:

	//Call a puts() function with argument to print the sentence you made to screen
	//CODE:

}