#include<stdio.h>
#include<ctype.h>



int main() {

	//Declare an int variable named exampleDataType. Initialize it to 'g'. 
	//CODE:

	//Use an if statement with isalpha() with example data type as arg.
	//CODE:

	//Use NESTED if with isupper() and exampleDataType arg. Test uppercase with print statement to cover scenario.
	//CODE:

	//Use an else that says _ is a lowercase letter.
	//CODE:

	//Use an else if with isdigit() arg to test if exampleDataType is digit. Print message.
	//CODE:


	//Use else statement that will cover if it is not a letter or a number/ Print message out to screen.
	//CODE: 
}